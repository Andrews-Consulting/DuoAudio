<Window x:Class="DuoAudio.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DuoAudio"
        mc:Ignorable="d"
        Title="DuoAudio Application" Height="500" Width="650"
        Closing="OnWindowClosing">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Menu Bar -->
        <Menu Grid.Row="0" Margin="0,0,0,0">
            <MenuItem Header="_Help">
                <MenuItem Header="_About" Click="OnAboutClick"/>
            </MenuItem>
        </Menu>

        <!-- Main Content -->
        <Grid Grid.Row="1" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Source Device -->
            <TextBlock Grid.Row="0" Text="Source Device (Audio Output):" FontWeight="Bold" Margin="0,0,0,5" FontSize="14"/>
            <ComboBox Grid.Row="1" x:Name="SourceDeviceComboBox" Margin="0,0,0,15" Padding="5" FontSize="12"/>

            <!-- Destination Device -->
            <TextBlock Grid.Row="2" Text="Destination Device:" FontWeight="Bold" Margin="0,0,0,5" FontSize="14"/>
            <ComboBox Grid.Row="3" x:Name="DestinationDeviceComboBox" Margin="0,0,0,15" Padding="5" FontSize="12"/>

            <!-- Buffer Configuration -->
            <Grid Grid.Row="4" Margin="0,0,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Buffer Configuration:" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,10,0" ToolTip="Adjusts buffer size for latency vs. stability trade-off"/>
                <Slider Grid.Column="1" x:Name="BufferConfigSlider" Minimum="1" Maximum="5" Value="3" TickFrequency="1" TickPlacement="BottomRight" ToolTip="1=Low Latency, 3=Balanced, 5=High Stability"/>
                <TextBlock Grid.Column="2" x:Name="BufferConfigValue" Text="Balanced" Margin="10,0,0,0" VerticalAlignment="Center" Width="100"/>
            </Grid>

            <!-- Control Button (Single Toggle Button) -->
            <Button Grid.Row="5" x:Name="ToggleWorkerButton" Content="Start Worker Task" Width="150" Padding="10,8" Margin="0,0,0,15" FontSize="12" FontWeight="Bold" HorizontalAlignment="Left" Click="OnToggleWorkerButtonClick"/>

            <!-- Worker Status Indicator -->
            <Border Grid.Row="6" x:Name="WorkerStatusBorder" Background="LightGray" Padding="10,5" Margin="0,0,0,15" CornerRadius="3" HorizontalAlignment="Left">
                <TextBlock x:Name="WorkerStatusTextBlock" Text="Worker Status: Stopped" FontWeight="Bold" FontSize="12"/>
            </Border>

            <!-- Status -->
            <TextBlock Grid.Row="7" x:Name="StatusTextBlock" Text="Status: Idle" FontWeight="Bold" Margin="0,0,0,15" FontSize="12" Foreground="DarkBlue"/>

            <!-- Bluetooth Section -->
            <Grid Grid.Row="8" Margin="0,0,0,5">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Bluetooth Auto-Reconnect -->
                <CheckBox Grid.Row="0" x:Name="AutoReconnectCheckBox" Content="Auto-Reconnect Bluetooth" Margin="0,0,0,5" FontSize="12"/>

                <!-- Bluetooth Status -->
                <TextBlock Grid.Row="1" x:Name="BluetoothStatusTextBlock" Text="BT Status: Unknown" FontWeight="Normal" FontSize="11" Margin="20,0,0,0" Foreground="Gray"/>
            </Grid>

            <!-- Run at Startup -->
            <CheckBox Grid.Row="9" x:Name="RunAtStartupCheckBox" Content="Run at Startup" Margin="0,0,0,15" FontSize="12" IsEnabled="False" Checked="OnRunAtStartupChecked" Unchecked="OnRunAtStartupUnchecked"/>
        </Grid>
    </Grid>
</Window>
